# Window Demo

Demonstrates generation and application of a window function to a discrete signal.
The demo saves the original signal, the window coefficients, and the weighted signal.

Build:
```
make window
```

Run:
```
cd output
./window_demo
```

Plot from the project root:
```
gnuplot scripts/window_demo.gp
```
